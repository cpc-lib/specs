# SPEC Validation Scripts

Run from repository root:

```bash
python scripts/validate_spec.py
python scripts/generate_manifest.py
```

`validate_spec.py` performs deterministic offline checks only; it does not contact production systems or external providers.
