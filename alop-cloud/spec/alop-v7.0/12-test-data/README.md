# Canonical Test Data
These fixtures are contract examples, not production seed data. Codegen tasks should convert them into JUnit/Testcontainers fixtures and keep expected amounts exact with `BigDecimal`.
