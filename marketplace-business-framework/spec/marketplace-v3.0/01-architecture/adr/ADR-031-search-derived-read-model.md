# ADR-031 — Search is Derived Read Model
MySQL remains truth. Elasticsearch/OpenSearch is rebuilt from versioned domain events. Search write failure never rolls back the originating product/price transaction.
