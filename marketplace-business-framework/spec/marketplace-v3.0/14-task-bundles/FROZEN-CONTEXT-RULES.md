# Task Bundle Frozen Context Rules

Every task bundle inherits V3.0 Frozen Contract.
Do not paste the entire repository into one model context.

Load:
global frozen docs
+ exact domain spec
+ exact Flyway migrations
+ exact OpenAPI
+ exact relevant events
+ matrices
+ tests
+ one TASK.

If generation needs an undefined cross-domain behavior, stop that part with `SPEC-GAP`.
