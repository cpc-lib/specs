# iam-dependencies

Published dependency-management view of the backend build baseline. Backend
modules inherit the same managed set from the reactor parent; independent
consumers may import this BOM.

No application code belongs in this module.
